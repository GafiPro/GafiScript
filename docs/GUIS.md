# GUI API

A GUI do editor é implementada pelo próprio mod.

A API para scripts criarem inventários personalizados permanece em desenvolvimento. A plataforma não vai fingir que uma GUI API existe só porque o editor já é uma GUI.
